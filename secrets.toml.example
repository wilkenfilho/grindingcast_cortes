# Copie este arquivo para .streamlit/secrets.toml (mesmo nome de pasta,
# sem o ".example" no final) e coloque sua chave real da Groq.
#
# .streamlit/secrets.toml NUNCA deve ser commitado no git — adicione-o
# no seu .gitignore.

GROQ_API_KEY = "sua_chave_aqui"
